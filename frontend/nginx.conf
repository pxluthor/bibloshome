server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # index.html — nunca cacheia (sempre busca do servidor)
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        expires 0;
    }

    # JS, CSS, imagens com hash no nome — cache eterno (conteúdo nunca muda)
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # SPA fallback — demais rotas servem index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
